<UserControl x:Class="Luminous.TimeSavers.BrowserControl.ToolWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

    Background="{DynamicResource ToolWindowBackgroundBrushKey}"
    Foreground="{DynamicResource ToolWindowTextBrushKey}"

    xmlns:bc="clr-namespace:Luminous.TimeSavers.BrowserControl"

    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="300"
    x:Name="BrowserControlToolWindow">

    <UserControl.DataContext>
        <bc:ViewModel />
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
    </Grid>
</UserControl>